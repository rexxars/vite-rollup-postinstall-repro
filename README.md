Run `npm install` with scripts being enabled, and see the issue:

```sh
$ npm install --save vite
npm error code 127
npm error path /Users/espen/vite-test/node_modules/rollup
npm error command failed
npm error command sh -c patch-package
npm error sh: patch-package: command not found
```
